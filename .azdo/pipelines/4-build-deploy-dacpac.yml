# ------------------------------------------------------------------------------------------------------------------------
# Pipeline to build and deploy Database Objects Only to existing resources
# See readme file for info about variable group "CheckList.Demo"
# ------------------------------------------------------------------------------------------------------------------------
name: $(date:yyyy).$(date:MM).$(date:dd)$(rev:.r) ${{ parameters.deployToEnvironment }}
pool:
  vmImage: windows-latest # dacpac restore only runs on Windows...

# ----------------------------------------------------------------------------------------------------
trigger:
  # since this repo has both AzDO and GHA pipelines, nothing is run automatically...
  - none
  # batch: true
  # branches:
  #   include:
  #     - main
  # paths:
  #   include:
  #     - 'src\CheckList.Database/*'
  #   exclude: 
  #     - '**/*.yml'
  #     - '**/*.yaml'
  #     - '**/*.bicep'
  #     - '**/*.md'

# ----------------------------------------------------------------------------------------------------
parameters:
  - name: deployToEnvironment
    displayName: Deploy To
    type: string
    values:
      - DEV
      - QA
      - PROD
      - DEV-QA-PROD
    default: DEV
  - name: loginAccountType
    displayName: Login Type
    type: string
    values:
      - SVC_PRINCIPAL
      - LOCAL_SQL
    default: SVC_PRINCIPAL
  - name: addFirewallRule
    displayName: Temporarily add agent IP to SQL firewall?
    type: boolean
    default: true

# ----------------------------------------------------------------------------------------------------
variables:
  - group: CheckList.Demo
  - template: vars/var-common.yml
  - template: vars/var-service-connections.yml

# ----------------------------------------------------------------------------------------------------
stages:
- ${{ if ne(parameters.deployToEnvironment, 'DEV-QA-PROD') }}:
  - template: stages/dacpac-deploy-stages.yml
    parameters:
      environments: ['DEV']
      singleEnvironment: 'true'
      appFolderName: $(sqlAppFolderName)
      appSolutionName: $(sqlAppSolutionName)
      appProjectName: $(sqlAppProjectName)
      loginAccountType: ${{ parameters.loginAccountType }}
      addFirewallRule: ${{ parameters.addFirewallRule }}

- ${{ if eq(parameters.deployToEnvironment, 'DEV-QA-PROD') }}:
  - template: stages/dacpac-deploy-stages.yml
    parameters:
      environments: ['DEV','QA','PROD']
      appFolderName: $(sqlAppFolderName)
      appSolutionName: $(sqlAppSolutionName)
      appProjectName: $(sqlAppProjectName)
      loginAccountType: ${{ parameters.loginAccountType }}
      addFirewallRule: ${{ parameters.addFirewallRule }}
